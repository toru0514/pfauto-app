# スプレッドシート構成ガイド

## 概要

商品データはGoogleスプレッドシートで管理し、各プラットフォーム（Creema, minne, BASE, iichi）への出品を自動化します。

---

## シート構成

### 共通シート（メイン）

**全ての商品情報を管理するメインシート**です。基本的にここだけで完結します。

- 商品の基本情報（タイトル、説明、価格、在庫など）
- 各プラットフォームのカテゴリ設定（プルダウンで選択）
- 出品先の指定
- 画像URL、配送情報など

### PFシート（Creema, minne, BASE, iichi）

各プラットフォーム固有の情報を管理するシートです。

- **基本的には編集不要** - 共通シートの値を数式（`='共通'!XX`）で参照
- プラットフォーム固有の値で上書きしたい場合のみ、数式を削除して直接入力
- 上書きしたセルは**黄色でハイライト表示**されます
- ステータス列（`*_status`, `*_last_synced_at`等）は各PFシートで直接管理

---

## カテゴリ列の使い方

共通シートのカテゴリ列は**プルダウン**で選択できます。

### Creema

| 列名 | 説明 | 入力方法 |
|------|------|----------|
| `creema_category_level1_label` | 大カテゴリ（16種類） | プルダウンから選択 |
| `creema_category_level2_label` | 中カテゴリ（243種類） | プルダウンから選択 |
| `creema_category_level3_label` | 小カテゴリ | 自由入力（任意） |

### minne

| 列名 | 説明 | 入力方法 |
|------|------|----------|
| `minne_category_parent_label` | 大カテゴリ（19種類） | プルダウンから選択 |
| `minne_category_label` | 小カテゴリ（236種類） | プルダウンから選択 |

### iichi（後日対応予定）

| 列名 | 説明 | 入力方法 |
|------|------|----------|
| `iichi_category_parent_label` | 大カテゴリ | 自由入力 |
| `iichi_category_child_label` | 小カテゴリ | 自由入力 |

---

## 運用フロー

### 1. 共通シートで商品を登録

1. 基本情報を入力（タイトル、説明、価格、在庫）
2. **カテゴリをプルダウンから選択**
3. 出品先を指定（例: `Creema,minne`）
4. 画像URLを設定

### 2. PFシートで確認（任意）

- 各PFシートを開くと、共通シートの値が自動で反映されている
- プラットフォーム固有の調整が必要な場合のみ編集

### 3. 自動化を実行

```bash
# Creema
pnpm test:e2e -- --grep creema

# minne
pnpm test:e2e -- --grep minne
```

カテゴリは**ラベルからIDに自動変換**されます。

---

## PFシートでの上書き

共通シートの値をプラットフォームごとに変えたい場合：

1. PFシートの該当セルを選択
2. 数式（`='共通'!XX`）を**削除**
3. 新しい値を直接入力
4. セルが**黄色**になり、上書き状態であることが分かります

**元に戻す場合:** `setup-platform-sheets.ts` を再実行

---

## スクリプト

### カテゴリプルダウンの設定

```bash
npx tsx scripts/setup-category-dropdowns.ts
```

共通シートのカテゴリ列にプルダウン（データ入力規則）を設定します。

### PFシートの数式設定

```bash
npx tsx scripts/setup-platform-sheets.ts
```

各PFシートのヘッダーと共通シート参照の数式を設定します。

### 動作確認

```bash
npx tsx scripts/test-sheets.ts
```

マルチシート読み込みの動作を確認します。

---

## 技術詳細

### カテゴリマスターデータ

| ファイル | 内容 |
|----------|------|
| `playwright/tests/creema-categories.ts` | Creemaカテゴリツリー（16大カテゴリ、243小カテゴリ） |
| `playwright/tests/minne-categories.ts` | minneカテゴリツリー（19大カテゴリ、236小カテゴリ） |

### ラベル→ID変換

自動化スクリプト実行時、ラベルからIDへの変換が自動で行われます：

| プラットフォーム | 変換関数 |
|------------------|----------|
| Creema | `resolveCreemaCategoryPath()` |
| minne | `resolveMinneParentIdByLabel()`, `resolveMinneChildIdByLabel()` |

### データの流れ

```
共通シート (ラベル)
    ↓ 数式参照
PFシート (ラベル)
    ↓ 自動化スクリプト（ラベル→ID変換）
APIリクエスト (ID)
```

---

## 共通シートの列構成

### 基本情報

| 列名 | 説明 |
|------|------|
| `product_id` | 商品ID（一意の識別子） |
| `sku` | SKUコード |
| `title` | 商品名 |
| `description` | 商品説明 |
| `price` | 価格 |
| `inventory` | 在庫数 |
| `material` | 素材 |
| `size_notes` | サイズ備考 |
| `weight_grams` | 重量（グラム） |
| `tags` | タグ（カンマ区切り） |
| `image_urls` | 画像URL（カンマ区切り） |

### 配送情報

| 列名 | 説明 |
|------|------|
| `production_lead_time_days` | 制作日数 |
| `shipping_fee` | 送料 |
| `shipping_method` | 配送方法 |
| `shipping_origin_pref` | 発送元都道府県 |

### カテゴリ（プルダウン）

| 列名 | 説明 |
|------|------|
| `creema_category_level1_label` | Creema大カテゴリ |
| `creema_category_level2_label` | Creema中カテゴリ |
| `creema_category_level3_label` | Creema小カテゴリ |
| `creema_color_ids` | Creemaカラー |
| `minne_category_parent_label` | minne大カテゴリ |
| `minne_category_label` | minne小カテゴリ |
| `minne_shipping_additional_fee` | minne追加送料 |
| `iichi_category_parent_label` | iichi大カテゴリ |
| `iichi_category_child_label` | iichi小カテゴリ |

### 管理情報

| 列名 | 説明 |
|------|------|
| `出品先` | 出品先（カンマ区切り: `Creema,minne`） |
| `ステータス` | 全体ステータス |
| `最終同期` | 最終同期日時 |
| `エラーメモ` | エラー情報 |
| `notes_internal` | 内部メモ |

---

## ステータス値

### 商品ステータス（`ステータス`列）

| 値 | 説明 |
|----|------|
| `new` / `新規` | 新規登録 |
| `ready` / `下書き準備済み` | 同期準備完了 |
| `queued` / `待機中` | キュー待ち |
| `processing` / `処理中` | 処理中 |
| `drafted` / `下書き作成済み` | 下書き完了 |
| `error` / `エラー` | エラー発生 |
| `skipped` / `スキップ` | 対象外 |

### ジョブステータス（各PFの`*_status`列）

| 値 | 説明 |
|----|------|
| `queued` | 待機中 |
| `processing` | 処理中 |
| `success` | 成功 |
| `error` | エラー |
| `skipped` | スキップ |
