# 商品追加機能の実装計画

## Context

現在、商品データはGoogle Sheetsに直接記載して管理しており、アプリ（ダッシュボード）は読み取り・ステータス更新のみ。新しい商品を追加するにはスプレッドシートを直接編集する必要がある。この機能により、アプリ内のフォームから最小限のフィールドで新商品をスプレッドシートに追加できるようにする。

## 入力フィールド（最小限）

- **商品ID** (必須) - テキスト入力 + 自動生成ボタン (`prod-{timestamp}`)
- **商品名** (必須) - テキスト入力
- **価格** (任意) - 数値入力
- **在庫** (任意) - 数値入力
- **出品先** (必須、1つ以上) - Creema / minne / BASE / iichi のチェックボックス

ステータスは自動的に `"new"` に設定される。

## 実装手順

### Step 1: Port層 - 型とインターフェース追加

**`application/ports/product-repository-port.ts`**

- `AddProductInput` 型を追加（productId, title, price, inventory, platforms）
- `ProductRepositoryPort` に `addProduct(input: AddProductInput): Promise<void>` メソッドを追加

### Step 2: Adapter層 - Google Sheets書き込み実装

**`adapters/google-sheets/product-repository.ts`**

- `GoogleSheetsProductRepository` に `addProduct` メソッドを実装
- `fetchSheetMatrix()` でヘッダー行を取得し、`findColumnIndex()` + `HEADER_ALIASES` で各フィールドを正しいカラム位置にマッピング
- `sheets.spreadsheets.values.append()` で最終行に追加（行番号計算不要、競合も回避）
- syncStatus列には `"new"` を自動設定
- Mock mode (`USE_MOCK_SHEETS_DATA=true`) ではスキップ（既存パターンと同じ）
- 既存の `rateLimiter.acquire()` を使用

### Step 3: UseCase層 - バリデーションとビジネスロジック

**`application/usecases/dashboard.ts`**

- `addProductUseCase` 関数を追加
- バリデーション: 商品ID必須、商品名必須、出品先1つ以上
- `findProductById` で商品IDの重複チェック
- platforms を正規化（trim, lowercase）

### Step 4: Server Action追加

**`app/dashboard/actions.ts`**

- `addProduct` サーバーアクションを追加
- `addProductUseCase` を呼び出し、`revalidatePath` でダッシュボードを更新（既存パターンと同じ）

### Step 5: UIモーダルコンポーネント作成

**`components/dashboard/add-product-modal.tsx`** (新規ファイル)

- Props: `open`, `pending`, `onSubmit`, `onClose`
- 各フィールドの `useState` 管理
- 商品ID自動生成ボタン
- プラットフォーム選択チェックボックス
- 送信ボタン（pending中は無効化）
- 既存の `DetailSheet` と同じモーダルオーバーレイスタイル

### Step 6: ダッシュボード統合

**`components/dashboard/dashboard-content.tsx`**

- ヘッダーに「商品を追加」ボタンを追加
- `showAddProductModal` / `pendingAddProduct` の状態管理
- `handleAddProduct` でサーバーアクション呼び出し + トースト通知 + 操作ログ
- 成功時: モーダルを閉じ、ダッシュボードが自動更新
- 失敗時: モーダルは開いたまま、エラーをトースト表示
- `OperationLogEntry.type` に `"add"` を追加、`renderOperationLabel` に `"商品追加"` を追加

## 対象ファイル一覧

| ファイル | 変更内容 |
|---------|---------|
| `application/ports/product-repository-port.ts` | `AddProductInput` 型 + ポートメソッド追加 |
| `adapters/google-sheets/product-repository.ts` | `addProduct` メソッド実装 |
| `application/usecases/dashboard.ts` | `addProductUseCase` 追加 |
| `app/dashboard/actions.ts` | `addProduct` サーバーアクション追加 |
| `components/dashboard/add-product-modal.tsx` | 新規作成: 追加フォームモーダル |
| `components/dashboard/dashboard-content.tsx` | ボタン・状態・ハンドラー・ログ統合 |

## 検証方法

1. `pnpm build` でビルドエラーがないことを確認
2. `pnpm test:unit` で既存テストが通ることを確認
3. Mock mode (`USE_MOCK_SHEETS_DATA=true`) でダッシュボードに「商品を追加」ボタンが表示されることを確認
4. モーダルを開き、バリデーション（空のID・タイトル、出品先未選択）でエラーが出ることを確認
5. 実際のGoogle Sheets接続環境で、追加した商品がスプレッドシートの最終行に反映されることを確認
