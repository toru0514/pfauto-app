# スプレッドシート項目 × Playwright 使用状況 調査レポート

**調査日**: 2026-03-16
**対象**: Google Sheets スキーマ（README定義 + テンプレート例）と Playwright テスト 4PF分

---

## 調査の目的

スプレッドシートに定義されている項目のうち、Playwright 自動化テストおよびアプリケーションで実際に使われていないカラムを特定し、不要項目の削除と必要項目の追加を検討する。

---

## 1. 不要な項目（削除候補）

### Playwright・アプリ共に未参照

| カラム名 | README定義 | 理由 |
|---|---|---|
| `sku` | 共通列 | 在庫管理用として定義されているが、4PF すべてのテストおよびアプリコードで一切参照されていない |
| `variant_options` | 共通列 | JSON形式のバリエーション構成。Creema/minne/BASE/iichi いずれのテストでも未実装。入力の手間だけ発生する |

### READMEスキーマ表に定義があるが実装されていない

| カラム名 | README記載箇所 | 理由 |
|---|---|---|
| `creema_shipping_profile_id` | PF固有列セクション | テストで未使用。配送方法は `shipping_method` からIDを解決している |
| `creema_handling_time_days` | PF固有列セクション | テストで未使用。`production_lead_time_days` / `creema_craft_period_days` が代替 |
| `creema_shop_section` | PF固有列セクション | テストで一切参照なし |
| `minne_required_options` | PF固有列セクション | テストで一切参照なし |

### 判断保留

| カラム名 | 備考 |
|---|---|
| `notes_internal` | 自動化には不要だが、作業者の運用メモとして残す価値あり。`updateProductStatuses` でnote書き込み機能はある |

---

## 2. 追加が必要な項目（スプシに存在しない可能性）

Playwright テストが `product.raw["column_name"]` で参照しているが、README のスキーマ表・テンプレート例に未定義の項目。

### Creema 固有オーバーライド列

| カラム名 | 参照箇所 | 説明 |
|---|---|---|
| `creema_material_id` | `creema-draft.spec.ts:298` | 素材ID直接指定。ラベルからの推測より確実 |
| `creema_material_label` | `creema-draft.spec.ts:301` | 素材名テキスト。IDが未設定時にエイリアスマッチでIDを推測 |
| `creema_shipping_origin_pref` | `creema-draft.spec.ts:308` | Creema固有の発送元都道府県（共通 `shipping_origin_pref` と異なる場合） |
| `creema_shipping_method` | `creema-draft.spec.ts:316` | Creema固有の配送方法名 |
| `creema_craft_period_days` | `creema-draft.spec.ts:325` | 制作日数のCreema固有上書き |
| `creema_size_free_input` | `creema-draft.spec.ts:335` | サイズ自由入力テキスト |
| `creema_weight_grams` | `creema-draft.spec.ts:340` | 重量(g)のCreema固有上書き |

### minne 固有オーバーライド列

| カラム名 | 参照箇所 | 説明 |
|---|---|---|
| `minne_title` | `minne-draft.spec.ts:124` | minne固有タイトル |
| `minne_description` | `minne-draft.spec.ts:128` | minne固有説明文 |
| `minne_price` | `minne-draft.spec.ts:134` | minne固有価格 |
| `minne_inventory` | `minne-draft.spec.ts:143` | minne固有在庫数 |
| `minne_shipping_days` | `minne-draft.spec.ts:149` | 発送までの日数 |

### iichi 固有オーバーライド列

| カラム名 | 参照箇所 | 説明 |
|---|---|---|
| `iichi_title` | `iichi-draft.spec.ts:135` | iichi固有タイトル |
| `iichi_description` | `iichi-draft.spec.ts:139` | iichi固有説明文 |
| `iichi_price` | `iichi-draft.spec.ts:144` | iichi固有価格 |
| `iichi_stock` | `iichi-draft.spec.ts:155` | iichi固有在庫数 |

### ステータス / トラッキング列（テンプレートに不足）

テンプレート例には `minne_*` のステータス列はあるが、他PFのステータス列が欠落。

| カラム名 | 説明 |
|---|---|
| `creema_status` | Creemaジョブステータス（モックデータには存在するがテンプレートにない） |
| `creema_last_synced_at` | Creema最終同期日時 |
| `creema_last_error` | Creema最新エラー |
| `base_status` | BASEジョブステータス |
| `base_last_synced_at` | BASE最終同期日時 |
| `base_last_error` | BASE最新エラー |
| `base_last_job_started_at` | BASEジョブ開始日時 |
| `base_last_duration_seconds` | BASE処理時間 |
| `base_attempt` | BASEリトライ回数 |
| `iichi_status` | iichiジョブステータス |
| `iichi_last_synced_at` | iichi最終同期日時 |
| `iichi_last_error` | iichi最新エラー |
| `iichi_last_job_started_at` | iichiジョブ開始日時 |
| `iichi_last_duration_seconds` | iichi処理時間 |
| `iichi_attempt` | iichiリトライ回数 |

---

## 3. 使用中の項目一覧（現状維持）

### 共通列

| カラム名 | 使用PF | 備考 |
|---|---|---|
| `product_id` | 全PF | 行の一意キー。リポジトリで使用 |
| `title` | 全PF | 共通タイトル。PF固有列がある場合はフォールバック |
| `description` | 全PF | 共通説明文 |
| `price` | 全PF | 共通価格 |
| `inventory` | 全PF | 共通在庫数 |
| `material` | Creema, iichi | 素材名。Creema素材IDの推測、iichiドロップダウン選択に使用 |
| `size_notes` | Creema | サイズ自由入力のフォールバック |
| `weight_grams` | Creema | 重量。サイズ入力テキストに付加 |
| `tags` | Creema | キーワードタグ（現在タグ入力は一時停止中） |
| `image_urls` | 全PF | 共通画像URL。PF固有列がある場合はフォールバック |
| `category_common` | Creema, iichi | 共通カテゴリラベル。iichi親カテゴリのフォールバック |
| `production_lead_time_days` | Creema, minne | 制作期間（日数） |
| `shipping_fee` | minne | 送料 |
| `shipping_method` | Creema, minne, iichi | 配送方法名 |
| `shipping_origin_pref` | Creema, minne | 発送元都道府県 |
| `出品先` | 全PF | プラットフォームフィルタ。リポジトリで使用 |
| `ステータス` | 全PF | sync_status。リポジトリで使用 |

### Creema 固有列

| カラム名 | 備考 |
|---|---|
| `creema_category_level1_label` | 第1階層カテゴリ名。ラベルからID解決 |
| `creema_category_level2_label` | 第2階層カテゴリ名。iichi子カテゴリのフォールバックとしても使用 |
| `creema_category_level3_label` | 第3階層カテゴリ名 |
| `creema_category_id` | 第1階層カテゴリID（直接指定） |
| `creema_category2_id` | 第2階層カテゴリID（直接指定） |
| `creema_category3_id` | 第3階層カテゴリID（直接指定） |
| `creema_color_ids` | カラーID（カンマ区切り） |

### minne 固有列

| カラム名 | 備考 |
|---|---|
| `minne_category_parent_id` | 大カテゴリID |
| `minne_category_id` | 小カテゴリID |
| `minne_image_urls` | minne固有画像URL |
| `minne_shipping_method` | 配送方法 |
| `minne_shipping_area` | 配送エリア |
| `minne_shipping_fee` | 送料 |
| `minne_shipping_additional_fee` | 追加送料 |

### BASE 固有列

| カラム名 | 備考 |
|---|---|
| `base_title` | BASE固有タイトル |
| `base_description` | BASE固有説明文 |
| `base_price` | BASE固有価格 |
| `base_inventory` | BASE固有在庫数 |
| `base_image_urls` | BASE固有画像URL |

### iichi 固有列

| カラム名 | 備考 |
|---|---|
| `iichi_category_parent_label` | 親カテゴリラベル |
| `iichi_category_child_label` | 子カテゴリラベル |
| `iichi_material_label` | 素材ラベル |
| `iichi_shipping_method_label` | 配送方法ラベル |
| `iichi_image_urls` | iichi固有画像URL |

### ステータス / トラッキング列（テンプレートに存在するもの）

| カラム名 | 備考 |
|---|---|
| `creema_last_job_started_at` | ジョブ開始日時 |
| `creema_last_duration_seconds` | 処理時間 |
| `creema_attempt` | リトライ回数 |
| `minne_status` | ジョブステータス |
| `minne_last_synced_at` | 最終同期日時 |
| `minne_last_error` | 最新エラー |
| `minne_last_job_started_at` | ジョブ開始日時 |
| `minne_last_duration_seconds` | 処理時間 |
| `minne_attempt` | リトライ回数 |

---

## 4. 次のアクション

### 優先度高

1. **スプシのヘッダー行を実際に確認する**
   - `.env.local` に `GOOGLE_SERVICE_ACCOUNT_BASE64` と `GOOGLE_SHEETS_SPREADSHEET_ID` を設定
   - `pnpm tsx scripts/test-sheets.ts` でヘッダー行を取得し、この調査結果と突合する
2. **不要列の削除**: `sku`, `variant_options` をスプシから削除（データがなければ）
3. **不足列の追加**: セクション2の追加候補をスプシに追加

### 優先度中

4. **README スキーマ表の更新**: 実態と合わせてスキーマ表・テンプレート例を修正
5. **未実装のREADME定義を削除**: `creema_shipping_profile_id`, `creema_handling_time_days`, `creema_shop_section`, `minne_required_options`

### 優先度低

6. **Playwright コード内のフォールバック列名の整理**: 冗長なエイリアス（`creema_category_level1_name`, `creema_category_level1` など）を削減し、正規の列名に統一する

---

## 補足: 調査方法

- `adapters/google-sheets/product-repository.ts` のヘッダーエイリアス定義を確認
- `playwright/tests/{creema,minne,base,iichi}-draft.spec.ts` の `product.raw["..."]` 参照を全数抽出
- README の「Google Sheets 項目定義」セクションおよびテンプレート例と突合
- Page Object (`playwright/tests/page-objects/*.ts`) のセレクタ定義を確認
